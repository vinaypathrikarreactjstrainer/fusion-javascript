// variables
let portfolio = document.querySelector('#portfolio'),
    ui = '', container;

// portfolio array of objects
const list = [
    { title : 'Happy Clients',score : 2121, iconName : `<i class="fa-regular fa-user"></i>`
    },
    { title : 'project completed',score : 2140, iconName : `<i class="fa-brands fa-stack-overflow"></i>`
    },
    { title : 'hour worked',score : 3245, iconName : `<i class="fa-regular fa-clock"></i>`
    },
    { title : 'Articles published',score : 2128, iconName : `<i class="fa-solid fa-pencil"></i>`
    }
];

const users = ["Anushka", "Shruti", "Akkamma", "Priyanka", "Muskan", "Sahil", "Shweta", "Rohini", "Anjali", "Sakshi"];

// generate portfolio ui
const getPortfolio = items => {
    // 1. iterate the items using loop
    for(let value of items){
        ui += `<div class="card">
                <h1>${value.iconName}</h1>
                <h2>${value.score}</h2>
                <p>${value.title}</p>
             </div>`
    }
    // portfolio.innerHTML = ui;
}
// call getPortfolio func on window load
// window.onload = 
getPortfolio(list);

let span = document.createElement('span');
span.classList.add('users');
span.textContent = users[1];
let div = document.createElement('div');

// span.textContent = 'vinay';

// array.forEach((items, index) => {})
    let userUI = ``
    // users.forEach((name, index) => {
    //     div.innerHTML = name
    // })
    container = document.querySelector('#container');
for(let x = 0; x < users.length; x++){
    let names = document.createElement('div');
    names.textContent = users[x];
    names.classList.add('box');
    container.appendChild(names);
}
// portfolio.append(span); 
// portfolio.append(div); 

// create dynamic element
let paragraph = document.createElement('p');
// styling & adding contents in p tag
paragraph.textContent = "This paragraph is generated by javascript";
paragraph.style.fontSize = "1.2em";
paragraph.style.color = "blue";
console.log('paragraph => ', paragraph);
// insert p tag inside to container
// container.appendChild(paragraph);
let bgImg = document.querySelector('#bgImg');
// get attribute
bgImg.getAttribute('src'); // will return the value of attribute
bgImg.setAttribute('height', '300px');